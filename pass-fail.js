// <!-- Create a program where:

// If a student scores 40 or more, they pass.
// If they score less than 40, they fail. -->


let studentScore = 0;

if(studentScore >=40){
    console.log("You are passed")
}
else{
    console.log("You are failed")
}